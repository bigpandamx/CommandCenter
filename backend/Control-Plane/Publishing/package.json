{
  "name": "@aegis-cc/publishing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Publishing: the shared entry point every analysis domain (Compliance, Threat Intelligence, eventually Risk Intelligence) calls to package approved intelligence for distribution to customer applications. A thin adapter over Announcements' existing draft/schedule/publish machinery, not a replacement for it -- see src/types.ts for the full reasoning.",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit -p ../../../tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
